export type CarouselInitialProps = {
  readyInMinutes: number;
  servings: number;
  title: string;
  image: string;
  sourceUrl: string;
  index: number;
};

export type DisplayHead2Props = {
  imgMenu: string;
  food: string;
  queryParams: string;
}

export type DisplayMenuFinalProps = {
  idnormal: string;
  title: string;
  image: string;
}
